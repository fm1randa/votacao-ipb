# Banco da eleição (dados do evento — nunca versionar)
votacao.db
votacao.db-shm
votacao.db-wal
*.db

# Binários de build (ancorados na raiz — sem isso, "votacao" casaria com
# qualquer diretório de mesmo nome, ex. o package Kotlin android/.../app/votacao)
/votacao-android
/votacao
/votacao.exe

# Artefatos locais
.playwright-mcp/
.DS_Store

# App Android (android/) — artefatos do Gradle e binário Go empacotado
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/app/src/main/jniLibs/
*.apk
